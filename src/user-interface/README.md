# FaaSTreams User Interface
Pure static JavaScript frontend fetching from the Data sink via a Data-sink FaaS service.


## serve locally:
python3 -m http.server 8080 to start local minimal front end to view data sink
